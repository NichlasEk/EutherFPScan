# Only fprintd uses the private libfprint; Debian's library stays installed.
[Unit]
Requires=eutherfpscan.service
After=eutherfpscan.service

[Service]
Environment=LD_LIBRARY_PATH=/opt/eutherfpscan/fprint
Environment=FP_EUTHER_VFS491=/run/eutherfpscan/control.sock
